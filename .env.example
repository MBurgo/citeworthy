# Citeworthy API keys. Copy to `.env` and fill in.
# Only ANTHROPIC_API_KEY is strictly required for v1 (rank).
# Components degrade gracefully with a clear message when an optional key is missing.

ANTHROPIC_API_KEY=
SERPAPI_API_KEY=
PERPLEXITY_API_KEY=
GEMINI_API_KEY=
